---
"saurus-extension": minor
---

Add `Saurus: Configure AI Model`, a provider-aware model picker that supports Copilot Chat discovery and provider-specific CLI model lists/fallbacks for Copilot CLI, Claude CLI, and Codex CLI. Includes a provider-default option, manual model entry, and safe rollback if updating `saurus.ai.model` fails.
