---
"saurus-extension": patch
---

Store Merriam-Webster API keys in VS Code Secret Storage instead of settings, migrate legacy settings keys automatically, and simplify suggestion menu empty states by removing per-source empty rows in favor of a single global no-suggestions message.
